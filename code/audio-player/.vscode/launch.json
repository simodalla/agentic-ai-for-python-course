{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start HTTP Server",
      "type": "node-terminal",
      "request": "launch",
      "command": "uv run python -m http.server -b 127.0.0.1 10000",
      "cwd": "${workspaceFolder}"
    }
  ]
}

